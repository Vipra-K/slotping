services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: slotping
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - slotping_postgres:/var/lib/postgresql/data

volumes:
  slotping_postgres:
